# Agentic Neurodata Conversion - Environment Variables
# Copy this file to .env and fill in your actual values

# Anthropic API Key (Required for LLM-powered correction analysis)
# Get your key from: https://console.anthropic.com
ANTHROPIC_API_KEY=

# Directory Configuration (Optional - defaults provided)
UPLOAD_DIR=./uploads
OUTPUT_DIR=./outputs
LOG_DIR=./logs

# Upload Limits (Optional)
MAX_UPLOAD_SIZE_GB=10

# Server Configuration (Optional)
HOST=0.0.0.0
PORT=8000

# Claude Code Configuration (Optional)
# Maximum output tokens for Claude responses (default: 32000, max: 200000)
CLAUDE_CODE_MAX_OUTPUT_TOKENS=200000

# ============================================================================
# Knowledge Graph Service Configuration
# ============================================================================

# Neo4j Database Configuration (Required for KG features)
# Two options for running Neo4j locally:
#
# Option 1: Docker Compose (Recommended)
#   - Run: docker-compose up -d
#   - Use password: dev-password (or change in docker-compose.yml)
#
# Option 2: Neo4j Desktop
#   - Install Neo4j Desktop from https://neo4j.com/download/
#   - Create a local database and set password
#   - Use the password you set in Neo4j Desktop

# Connection URI for Neo4j database
NEO4J_URI=bolt://localhost:7687
# Neo4j authentication
NEO4J_USER=neo4j
NEO4J_PASSWORD=
# Neo4j database name (default: neo4j, or nwb-kg-dev for development)
NEO4J_DATABASE=

# KG Service Configuration
# URL where KG service is running (default: http://localhost:8001)
KG_SERVICE_URL=http://localhost:8001
# Enable/disable KG service integration (true/false)
KG_SERVICE_ENABLED=true
# Timeout for KG service requests in seconds
KG_SERVICE_TIMEOUT=5.0
# Maximum retries for KG service requests
KG_MAX_RETRIES=2
